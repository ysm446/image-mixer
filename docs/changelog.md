# Changelog

作成日時: 2026-08-05 22:27
更新日時: 2026-08-07 22:47

## 未リリース

### 2026-08-07 22:47

- 設定アイコンを、外周の歯がはっきり見える歯車のライン(線画)デザインへ変更した。

### 2026-08-07 22:45

- 設定の「表示」セクションへ「背景のドットを表示」トグルを追加した。既定はオンで、設定は`localStorage`へ保存する。

### 2026-08-07 22:42

- キャンバス背景のドットをlm-graphと同じ明るさ(#394154、サイズ1.4)へ変更した。SVGパターン1枚での描画のため、ズームやノード数で負荷は増えない。
- `.react-flow__renderer`の不透明なグラデーション背景がドットのSVGレイヤーを覆い隠していたため、グラデーションを`.react-flow`ルート要素へ移してドットが見えるようにした。

### 2026-08-07 22:41

- 設定の「表示」セクションへ「ノードをスナップする」トグルを追加した。有効時はノードのドラッグが背景ドットと同じ22pxグリッドへスナップする。設定は`localStorage`へ保存する。

### 2026-08-07 22:40

- ノードのサイズ追従チェックボックスからグラデーションと内側ハイライトを外し、フラットな単色デザインへ変更した。

### 2026-08-07 22:39

- 上部バーのサイドバー開閉と設定のアイコンボタンから枠と背景を外し、アイコンのみの表示にした。

### 2026-08-07 22:38

- 上部バー左端に左サイドバーの表示・非表示を切り替えるボタンを追加した。状態は`localStorage`へ保存し、次回起動時も保持する。

### 2026-08-07 19:48

- 下部ステータスバー左端に生成の進行状況を表示するようにした。ComfyUIのWebSocketからステップ進捗を受け取り、実行中ノード名、進捗バー、パーセント、待機件数を表示する。Batch実行中も対象。セッション名表示はサイドバーと重複するため削除した。
- ノードのコピー・貼り付け、生成画像のコピー・保存、生成の完了・失敗・キャンセル、一括処理の完了をステータスバーへ約3秒間通知するようにした。

### 2026-08-07 19:28

- ルートフォルダを移動・改名すると画像が表示されなくなる問題を修正した。`session.json`内の画像・生成結果の絶対パスを、読み込み時に現在のセッションの`assets/`基準へ自動修復して保存し直す。
- 同じ状況でアセット自動削除が参照切れと誤判定し、セッション内の画像を削除してしまう問題を修正した。削除判定の前にパス修復を適用する。Batch出力(セッション外の結果)は修復対象外。

### 2026-08-07 19:23

- 設定ウインドウへ左サイドバーを追加し、「Local LLM」と「表示」のセクションへ分割した。タイトルは「設定」へ変更した。
- 「表示」セクションにミニマップ表示のオン/オフを追加した。変更は即座に反映され、`localStorage`へ保存して次回起動時も保持する。

### 2026-08-07 19:20

- TextノードのTEXT出力ピン横のラベル色が青のままだったのを、他のText系ノードと同じ紫へ修正した。

### 2026-08-07 19:16

- 下部ステータスバー左のComfyUI状態表示を削除し、現在のセッション名だけを表示するようにした。ComfyUIの状態は上部バーのボタンで確認できる。

### 2026-08-07 19:15

- ノード選択時の強調表示を改善した。従来の薄いリングに代えて、ノード種別のアクセント色による実質3px相当の太い外周線と外側グローを表示する。

### 2026-08-07 19:12

- 上部バーのモデル選択バーとLoad／Ejectボタンをバー中央へ配置した。バーの幅はモデル名の長さに合わせて伸縮し、長い名前は上限幅で省略する。
- 上部バーの高さを68pxから44pxへ縮小し、モデル選択バー、Load／Eject、ComfyUI、設定の各ボタンの高さを38pxから30pxへ揃えて縮小した。
- モデル選択バー(ロード済み表示)、Load／Ejectボタン、ComfyUIボタン、設定ボタンの配色を青系へ統一した。ComfyUI Ready時のドットも緑から青へ変更した。
- ROOT FOLDERボタンをクリックするとメニューを表示し、「フォルダを選択…」に加えて過去に開いたルートフォルダ(最大10件)を選んで切り替えられるようにした。履歴は`data/app-settings.json`へ保存する。

### 2026-08-07 17:26

- Text TransformノードのOutput欄の最小高さを180pxから100pxへ縮小した。内容が増えた場合は従来どおり自動で伸びる。

### 2026-08-07 16:54

- コードレビューで見つかった安定性の問題を修正した。
  - ウィンドウを閉じた後に生成完了などのイベント送信で破棄済みウィンドウへアクセスし、mainプロセスで例外が発生し得る問題を修正した。
  - ComfyUIの起動直後にプロセスが異常終了した場合でも90秒のタイムアウトまで待ち続け、終了理由のエラーメッセージが「Timed out」で上書きされる問題を修正した。
  - セッション削除時に、そのセッションの実行中・待機中の生成ジョブをキャンセルするようにした。削除済みフォルダへ生成結果が書き込まれてセッションフォルダが復活する問題を防ぐ。
  - ComfyUI起動前の存在チェックがpython.exeとmain.pyの実体をメモリへ読み込んでいたのを`access`による確認へ変更した。

### 2026-08-07 16:39

- モデル選択バーの「LOCAL LLM」とLoaded等の副題を削除し、パス部分とGGUF拡張子を除いたモデル名を等幅フォントで表示する単一行構成へ整理した。長い名前は先頭を残して末尾を省略する。
- モデルのLoad／Unloadをアイコンボタンへ変更し、ロード時はEjectアイコンを表示するようにした。
- ComfyUIボタンから状態説明文を削除し、モデル選択バーと同じ38pxの高さへ揃えた。

### 2026-08-07 16:34

- 上部のLocal LLMモデル選択を`lm-graph`風のクリック式バーへ変更し、ポップアップ一覧からモデルを選択して直接ロードできるようにした。
- モデルバーと一覧にロード状態を反映し、ロード済みは明るい緑、アンロードは暗色、切替中はアニメーション、再ロード待ちは黄系、エラーは赤系で表示するようにした。

### 2026-08-07 16:19

- Batch Image GenerateをImage Generateと同じオレンジ系へ変更し、ノード枠、フォルダ欄、進捗、結果枠、ミニマップの配色を統一した。

### 2026-08-07 16:14

- 画面上のPromptノードをTextへ改称し、既定タイトル、右クリックメニュー、入出力ラベルをTEXTへ統一した。既存セッション互換のため内部IDは維持し、旧既定タイトルは読込時にTextへ移行する。
- Text、Image Describe、Text Transformを同じ紫系のノードデザインへ統一した。Image Describeの画像入力ピンはIMAGE型を示す青のまま維持する。
- Image DescribeとText Transformのテキストエリアを、Textノードと同じ濃紺の背景・枠・文字色へ統一した。

### 2026-08-07 16:08

- Text系のピンとエッジを紫、Image系を青へ統一し、ノード配色と接続型の色を整理した。
- 既存セッションとコピーしたエッジも接続先の型に応じた色へ自動補正するようにした。

### 2026-08-07 16:00

- 任意の入力テキストをInstructionに従って翻訳・修正・詳細化・短縮できる`Text Transform`ノードを追加した。
- テキスト未接続時はInstructionだけから新しい文章や画像編集プロンプトを生成できるようにした。
- Text Transformのストリーミング表示、編集可能なPrompt出力、折りたたみSystem Prompt、既定値表示、経過時間、キャンセル、エラー表示を追加した。

### 2026-08-07 15:42

- 画像をLocal LLMで解析し、説明プロンプトをストリーミング生成する`Image Describe`ノードを追加した。
- Image Describeの出力を編集可能にし、Prompt型としてImage GenerateとBatch Image Generateへ接続できるようにした。
- 既定値を表示する折りたたみ式System Prompt、経過時間、キャンセル、エラー表示を追加した。
- WebP・BMPなどの画像も安定して解析できるよう、LLM送信前にPNGへ正規化するようにした。

### 2026-08-07 15:28

- 上部バーへLocal LLMのGGUFモデル選択とLoad／Reload／Unload操作を追加した。
- `runtime/llama-server/`のプロセス管理、`models/`のモデルと`mmproj`の自動検出、状態通知、設定保存を追加した。
- 右端の歯車からLocal LLMの生成・GPU・アイドルアンロード設定、フォルダ表示、再スキャンを操作できる設定画面を追加した。
- GGUFモデルを誤ってコミットしないように`models/`をGit管理対象外へ追加した。

### 2026-08-07 14:59

- キャンバスの空白部分へ画像をドロップした位置にImageノードを作成し、取り込んだ画像を自動で割り当てる操作を追加した。

### 2026-08-07 11:57

- Batch Image Generateノードへ生成中のリアルタイム経過時間表示を追加し、終了後も処理時間を確認できるようにした。

### 2026-08-07 05:58

- セッションごとにノードキャンバスの位置とズームを保存し、セッション切り替えやアプリ再起動後に同じ表示へ復元するようにした。
- Batch Image Generateノードの外部出力フォルダにある最新結果を維持したまま、セッションを複製できるようにした。

### 2026-08-07 05:52

- `Batch Image Generate`ノードを追加し、選択フォルダ直下の対応画像をImage 1として既存FIFOキューで1枚ずつ処理できるようにした。
- 入力フォルダ直下へ日時付き出力フォルダを作成し、生成結果と入力・出力・設定・エラーを記録した`batch-result.json`を保存するようにした。
- バッチの進捗、成功・失敗件数、現在のファイル、最新結果、キャンセル、出力フォルダを開く操作をノードへ追加した。

### 2026-08-07 05:29

- Imageノードなどを単独で貼り付けた際、接続先のImage Generateノードで使用済みの同一入力ピンへ境界エッジが重複する問題を修正した。

### 2026-08-07 02:47

- 選択ノードを同じセッションへコピー＆ペーストしたとき、未選択の元ノードにつながっていた境界エッジも維持するようにした。
- 別セッションへの貼り付けや接続相手が削除済みの場合は、存在しないノードへの境界エッジを作成しないようにした。

### 2026-08-07 02:43

- 右上のComfyUI状態表示をLoad／Unloadトグルボタンへ変更し、アプリ管理のComfyUIプロセスを画面から起動・停止できるようにした。
- 外部で起動されたComfyUIへ接続している場合は、安全のためUnloadを無効にして理由をツールチップへ表示するようにした。

### 2026-08-07 02:20

- 縦長画像の固有サイズが拡大枠を押し広げて画面下へはみ出す問題を修正し、画像を最大768pxかつウインドウ余白内の固定枠へ収めるようにした。

### 2026-08-07 02:13

- 左サイドバーの境界をドラッグして220〜520pxの範囲で幅を変更できるようにし、選択幅の保存とダブルクリックによる標準幅へのリセットに対応した。

### 2026-08-07 02:10

- 左サイドバーのセッション枠を見出しの左右位置へ揃え、三点メニューボタンを枠端から少し内側へ調整した。

### 2026-08-07 02:08

- 画像の拡大プレビューを利用可能なウインドウ領域へ直接制約し、最大768 × 768pxを維持しながら小さいウインドウでは余白内へ自動縮小するようにした。

### 2026-08-07 01:58

- 左サイドバーの新規セッション追加と三点メニューをSVGアイコンへ変更し、ボタン中央へ正確に配置した。

### 2026-08-07 01:56

- Image GenerateノードのImage 1サイズ追従チェックボックスを、ノード配色に合うカスタムデザインへ変更した。

### 2026-08-07 01:52

- ImageノードとImage Generateノードの拡大表示を、プレビュー画像の内部解像度に左右されない共通の768 × 768px枠へ統一した。

### 2026-08-07 01:33

- ImageノードとImage Generateノードの画像表示サイズを、共通の360 × 360px基準で計算するようにした。
- 同じ解像度・縦横比の入力画像と生成画像が、ノード種別によって異なる大きさで表示される問題を修正した。

### 2026-08-07 01:27

- Imageノードへ読み込んだ画像をクリックすると、生成結果と共通の画面内プレビューで拡大表示するようにした。
- 読込後の画像右上へ変更アイコンを追加し、ファイル選択による差し替えとドラッグ＆ドロップによる差し替えを引き続き利用できるようにした。

### 2026-08-07 01:22

- 生成ノードのUI名称をQwen固有の表現からモデル非依存の`Image Generate`へ変更した。画像接続時は`IMAGE EDIT`、未接続時は`IMAGE GENERATE`と表示する。
- 既存セッションの初期タイトル`Qwen Edit`と`Qwen composition`を、読み込み時に`Image Generate`へ移行するようにした。ユーザーが変更したタイトルは維持する。
- 上部バーから`Image Mixer`と説明文を削除し、ComfyUIの起動状態だけを右端へ表示するようにした。

### 2026-08-06 14:12

- 生成ノードのImage 1サイズ反映ボタンを、状態を保存できるチェックボックスへ変更した。
- チェック中はGenerateを押すたびにImage 1の最新解像度をWidth・Heightへ反映し、接続元が生成待ちの場合もその出力設定を引き継ぐようにした。

### 2026-08-06 13:57

- 左サイドバーのセッション一覧へ細く控えめなスクロールバーを適用した。
- 三点メニューをスクロール領域に切られない画面基準の配置へ変更し、画面下端付近ではボタンの上側へ開くようにした。

### 2026-08-06 13:46

- Prompt・Image・Generateノードのタイトル横へ編集アイコンを追加し、アイコンを押したときだけタイトルを編集できるようにした。
- タイトル編集はEnterまたはフォーカス移動で確定し、Escapeで取り消せるようにした。

### 2026-08-06 13:44

- Promptノードの本文入力欄を内容に合わせて自動拡張し、長文でも内部スクロールバーを表示しないようにした。

### 2026-08-06 13:23

- キュー内の後続生成が、実行直前に接続元ノードの最新生成結果を入力として解決するようにした。
- 先行生成に利用可能な結果がない場合は、後続処理を誤った画像生成モードで実行せずエラーにするようにした。

### 2026-08-06 13:04

- Generate操作をアプリ内FIFOキューへ追加し、複数の生成要求を常に1件ずつ順番に処理するようにした。
- 待機ノードへキュー状態と削除ボタンを表示し、待機中の削除または実行中のキャンセル後に次の生成を自動開始するようにした。

### 2026-08-06 12:45

- 左サイドバーのセッション三点メニューを開いたとき、後続セッションを押し下げず前面へ重ねて表示するようにした。


### 2026-08-06 12:31

- キャンバスの空白部分は通常の矢印カーソル、ノード本体は人差し指の手形カーソルを表示するように分けた。


### 2026-08-06 12:26

- ノードミニマップを暗いグラデーション背景、角丸、枠線、影を持つナビゲーションパネル風のデザインへ変更した。
- ノード種別ごとの塗りと輪郭、現在の表示範囲を示す青い枠、ホバー時の強調表示を追加した。


### 2026-08-06 03:08

- スクリーンショット保存後に、ファイル名と保存完了を示す通知を画面右下へ表示するようにした。
- 通知をクリックするとエクスプローラーで保存フォルダを開き、撮影したPNGを選択表示するようにした。
- 通知は6秒後または閉じるボタンで消えるようにした。


### 2026-08-06 03:06

- ノード本体へマウスを重ねたときの手形カーソルを、通常の矢印カーソルへ変更した。


### 2026-08-06 03:04

- F12キーでアプリのコンテンツ領域をPNG撮影し、選択中Root Folder直下の`screenshot/`へ日時付きファイル名で保存する機能を追加した。


### 2026-08-06 03:03

- アプリの初期ウィンドウサイズを、タイトルバーや外枠を除くコンテンツ領域で1920 × 1080に変更した。


### 2026-08-06 03:02

- ノード枠線の上辺だけを太くする指定を外し、四辺を均一な太さに揃えた。


### 2026-08-06 02:59

- Prompt・Image・Generateノードのアクセントカラーを上辺だけでなく外周全体の枠線へ適用した。
- ノード選択時も種類ごとの枠色を維持し、同色のグローで選択状態を強調するようにした。


### 2026-08-06 02:32

- 選択ノードとノード間エッジを、Ctrl+C / Ctrl+Vで別セッションへコピーできるようにした。
- 別セッションへの貼り付け時は、入力画像と生成結果を貼り付け先の`assets/`へ独立コピーしてパスを付け替えるようにした。
- 実行中ノードのコピーは、実体のない実行状態を引き継がず`canceled`として貼り付けるようにした。


### 2026-08-06 02:23

- セッション保存時に、ImageノードとQwen生成結果のどちらからも参照されていない画像を`assets`から自動削除するようにした。
- 既存セッションと複製先の未使用画像も整理し、取り込み中・生成結果保存中の画像は誤削除しないよう一時保護する。

### 2026-08-06 02:13

- Qwen生成ノードのWidth・Heightを、入力確定時と生成直前に64～4096の範囲内かつ最寄りの8の倍数へ自動補正するようにした。

### 2026-08-06 01:45

- アプリ名を`Image Edit`から`Image Mixer`へ変更した。
- パッケージ名、ウィンドウ・画面タイトル、preload API、ComfyUI一時アップロード先、起動メッセージ、README・仕様書を新名称へ統一した。
- データ保存先と既存ライブラリ設定は変更せず、従来のセッションを引き続き利用できるようにした。

### 2026-08-06 01:33

- エッジをクリックしたときに選択状態を青く強調表示するようにした。
- 選択したエッジをDeleteまたはBackspaceで確実に削除できるようにし、ダブルクリックでの削除にも対応した。

### 2026-08-06 01:29

- 画像入力のないQwen生成用workflowを追加し、Promptだけを接続した生成ノードでテキストから画像を生成できるようにした。
- 画像ピンの接続有無から画像生成・画像編集workflowを自動選択し、ノード表示を`QWEN GENERATE` / `QWEN EDIT`へ切り替えるようにした。
- 画像ピンが接続済みで画像が未読込の場合は、画像生成へ暗黙に切り替えず入力エラーを表示するようにした。

### 2026-08-06 01:20

- Qwen Editノード上部の設定欄までの余白を縮めた。
- Image 1の幅・高さを反映する操作をアイコン化し、Width・Height入力欄の右横へ移動した。

### 2026-08-06 01:18

- 新規セッションのデフォルトノードを、PromptからQwen EditのPrompt入力、ImageからImage 1入力へ接続済みの状態で作成するようにした。

### 2026-08-06 01:17

- 左サイドバーの見出し、ルートフォルダ情報、セッション名、日時、メニュー、補足文のフォントを一段階大きくした。

### 2026-08-06 01:16

- `Ctrl+V`で貼り付けるノード群を、現在表示しているキャンバスの中央付近へ配置するようにした。
- 複数ノードの相対位置を維持し、連続貼り付け時は少しずつずらして重なりを判別できるようにした。

### 2026-08-06 01:14

- セッションの三点メニューから名前を変更すると、一覧の名前部分をその場で編集できるようにした。
- Enterまたはフォーカス移動で確定し、Escapeで取り消せるようにした。

### 2026-08-06 00:28

- アプリ終了・更新などで生成処理だけが消え、ノードが`running`のまま残る問題を修正。
- セッション読込時に実行ジョブの有無を照合し、孤立した生成状態を`canceled`へ自動修復して保存するようにした。
- 実行中セッションの複製先にも孤立した`running`状態を残さないようにした。

### 2026-08-06 00:24

- Qwen EditノードのPrompt・Image 1～3・IMAGEピン文言をノードの外側へ移動。
- ピン文言用の内側余白を取り除き、生成結果の表示幅を維持したままノード幅を調整。

### 2026-08-06 00:23

- 上部ヘッダーのPrompt・Image・Generate新規作成ボタンを削除。
- ノード追加操作をキャンバスの右クリックメニューへ一本化し、ヘッダーのComfyUI状態を右端へ再配置。

### 2026-08-06 00:22

- Qwen Editノードの生成中に表示されるCancelボタンを追加。
- 生成前後のHTTP処理を中断し、ComfyUIへ投入済みの対象promptをjob cancel APIで停止するIPCを追加。
- キャンセル状態、キャンセルまでの経過時間、不完全な生成assetのクリーンアップに対応。

### 2026-08-06 00:18

- Qwen EditノードのSeed欄へサイコロアイコンのランダム化ボタンを追加。
- ボタンを押すたびに0～2147483647の新しいseedをノードへ設定するようにした。

### 2026-08-06 00:17

- 画面下部へステータスバーを追加し、ComfyUI状態と現在のセッションを表示。
- CPU・RAM・GPU・VRAMの使用量を定期更新するリソースバーを追加。
- 使用率65%超をオレンジ、85%超を赤で表示し、GPU情報を取得できない場合はGPU項目を自動的に省略。

### 2026-08-06 00:13

- Qwen Editノードの生成結果をクリックすると画面内へ拡大表示するプレビューを追加。
- 背景クリックまたはEscapeでプレビューを閉じる操作と、拡大画像への解像度表示を追加。

### 2026-08-06 00:11

- Qwen Editノードで生成中の経過時間をリアルタイム表示し、生成終了後も確定した時間を保持するようにした。

### 2026-08-06 00:10

- セッションの三点メニューへ「複製」を追加。
- グラフ、入力画像、生成結果を新しいUUIDのセッションフォルダへコピーし、画像パスを複製先へ再割り当てするようにした。
- 複製名の自動採番、複製後のセッション切り替え、失敗時の不完全フォルダ削除に対応。

### 2026-08-06 00:07

- `Ctrl+C` / `Ctrl+V`による選択ノードのコピー＆ペーストに対応。
- 複数選択時は選択ノード同士を結ぶエッジも、新しいノードIDへ接続し直して複製するようにした。
- 貼り付け後の一括選択、連続貼り付け位置のオフセット、同一セッション制約を追加。

### 2026-08-06 00:05

- Qwen Editノードの生成結果へコピーと保存アイコンを追加。
- 生成画像をWindowsクリップボードへ画像としてコピーできるようにした。
- 保存先ダイアログから生成画像を任意の場所へ書き出せるようにした。
- IPCで扱える画像パスをセッションライブラリ配下へ制限。

### 2026-08-06 00:01

- Electron標準のFile / Edit / Viewなどのアプリケーションメニューを削除。
- ウィンドウのメニューを明示的に解除し、Altキーを押しても再表示されないようにした。
- Qwen Editノードへ生成にかかった時間の表示を追加。
- ComfyUI呼び出しから結果保存または失敗までを計測し、セッションへ永続化するようにした。
- 経過時間を長さに応じてms、秒、分・秒で表示。

### 2026-08-05 23:58

- 範囲選択後に複数ノード全体を囲む矩形を非表示にした。
- ドラッグ中の選択範囲と各ノード自身の選択枠は引き続き表示する。

### 2026-08-05 23:57

- 左ドラッグによる部分一致の範囲選択と、選択ノードの一括移動に対応。
- `F`で選択ノードへフォーカスし、`A`ですべてのノードを表示するショートカットを追加。
- Prompt入力や名前変更などの入力欄ではキャンバスショートカットを無効化。

### 2026-08-05 23:56

- React Flowキャンバスの右クリックメニューを追加。
- Prompt / Image / Generateを選択し、右クリックしたグラフ座標へノードを配置できるようにした。

### 2026-08-05 23:54

- セッション項目へ三点メニューを追加し、「名前を変更」と「削除」を配置。
- 名前変更用のアプリ内モーダルと永続化IPCを追加し、`session.json`、一覧、アクティブセッションへ反映するようにした。

### 2026-08-05 23:51

- React Flowキャンバス右下の操作ボタン一式を削除。
- キャンバス下部のReact Flow attribution表示を非表示にした。

### 2026-08-05 23:50

- 新規セッションボタンを押しても反応しない問題を修正。
- Electronで非対応の`window.prompt()`を廃止し、重複しない`Session N`名を自動採番して即時作成するようにした。

### 2026-08-05 23:48

- Qwen Editノードの幅と結果表示領域が、設定解像度または生成結果の縦横比に合わせて変形するようにした。
- 極端な縦長・横長画像でも設定操作ができるよう、ノードの最小表示サイズを維持。

### 2026-08-05 23:46

- ルートプロジェクトをGitリポジトリとして初期化。
- 独立したGitリポジトリである`runtime/ComfyUI`と`*.tsbuildinfo`をルート側の追跡対象から除外。

### 2026-08-05 23:44

- READMEを追加し、アプリ概要、セットアップ、起動方法、基本操作、セッション保存、トラブルシューティングを記載。
- モデルとComfyUI本体のライセンスがアプリとは別であること、現在の未実装機能を明記。

### 2026-08-05 23:43

- WebP / BMPを選択またはドロップすると「The selected file is not a readable image」になる問題を修正。
- 画像デコードとサムネイル生成を`sharp`へ切り替え、PNG / JPEG / WebP / BMPを同じ処理で検証するようにした。

### 2026-08-05 23:33

- Imageノードを画像の縦横比に合わせて変形し、元画像の解像度を表示するようにした。
- Qwen Editノードに、Image 1の解像度をwidth / heightへ反映するボタンを追加した。
- 旧セッションの画像アセットにも読み込み時に解像度を補完するようにした。

### 2026-08-05 23:27

- Promptノードで日本語IME入力が重複・多重入力のようになる問題を修正。
- IME composition中はtextarea内のdraftを保持し、変換確定時だけノードデータへ反映するようにした。
- Prompt入力欄のkeyboard eventがReact Flowへ伝播しないようにした。

### 2026-08-05 23:26

- Electronアプリの二重起動を防止。
- 2回目の起動要求では既存ウィンドウを復元して前面表示するようにした。

### 2026-08-05 23:22

- 左サイドバーへルートフォルダ設定とセッション一覧を追加。
- セッションの新規作成、切り替え、確認付き削除に対応。
- セッションごとにグラフを自動保存し、入力画像と生成結果を個別の `assets/` へ保存するようにした。
- Imageノードの画像領域へのドラッグ＆ドロップ読み込みに対応。

### 2026-08-05 23:12

- 必要なローカル環境を確認して `npm run dev` を実行する `start.bat` を追加。
- ComfyUIの起動・停止はアプリ本体へ任せ、バッチから重複起動しない構成にした。

### 2026-08-05 23:10

- Electron + React + React Flowによる最初の動作版を追加。
- Promptノード、Imageノード、3画像入力対応Qwen Editノードを追加。
- Qwen Editの生成結果を次の生成ノードへ画像として接続できるようにした。
- width、height、seed、steps、CFGをAPI workflowへ上書きする処理を追加。
- ComfyUIの画像upload、prompt投入、history監視、生成結果保存を追加。
- アプリ専用ポート8189でComfyUIを自動起動・自動停止する処理を追加。
- API workflowへ3つ目の `LoadImage` と `image3` 接続を追加。
- `.venv`へComfyUI依存関係とCUDA 13対応PyTorchを導入。

### 2026-08-05 22:27

- ノード型画像編集アプリの目的、MVP、非目標を定義。
- Electron + React Flow とローカル ComfyUI を使う推奨アーキテクチャを定義。
- エディタのノード種別、接続規則、生成状態、永続化方針を文書化。
- `Qwen-Rapid-AIO.json` の構成とアプリから差し替えるパラメータを整理。
- ComfyUI の API workflow、画像 upload、prompt queue、WebSocket、history、result 取得の連携方針を文書化。
- Python `.venv` の初期方針と段階的な実装計画を追加。
